FROM python:3.8-slim
RUN pip install requests

COPY . .

ARG TEST_SCRIPT
ARG TEST_SCRIPT_ARG
ENV TEST_SCRIPT_ENV ${TEST_SCRIPT}
ENV TEST_SCRIPT_ARG_ENV ${TEST_SCRIPT_ARG}

RUN chmod 777 /*.py

CMD python ${TEST_SCRIPT_ENV} ${TEST_SCRIPT_ARG_ENV}